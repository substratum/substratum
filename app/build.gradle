apply plugin: 'com.android.application'

android {
    compileSdkVersion 24
    buildToolsVersion "24.0.1"

    lintOptions {
        checkReleaseBuilds false
        abortOnError false
    }

    applicationVariants.all { variant ->
        variant.outputs.each { output ->
            output.outputFile = new File(
                    output.outputFile.parent, "substratum_beta_testers" + versionCode + ".apk")
        }
    }

    defaultConfig {
        applicationId "projekt.substratum"
        minSdkVersion 23
        targetSdkVersion 24
        versionCode 216
        versionName "two hundred seventeen"
        renderscriptTargetApi 23
        renderscriptSupportModeEnabled true
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    testCompile 'junit:junit:4.12'

    /////////////////////////////////////
    // Crucial and Essential Libraries //
    /////////////////////////////////////

    // Google Support Libraries
    compile 'com.android.support:appcompat-v7:24.2.0'
    compile 'com.android.support:cardview-v7:24.2.0'
    compile 'com.android.support:recyclerview-v7:24.2.0'
    compile 'com.android.support:support-v13:24.2.0'
    compile 'com.android.support:design:24.2.0'
    compile 'com.android.support:palette-v7:24.2.0'
    compile 'com.android.support:preference-v14:24.2.0'

    // Google Firebase SDK
    compile 'com.google.firebase:firebase-messaging:9.4.0'

    // Butter Knife (remain on 7.0.1 for standalone library)
    compile 'com.jakewharton:butterknife:7.0.1'

    // Image Downloading and Caching
    compile project(':glide')
    compile project(':picasso')

    // Substrate Tools
    compile project(':substrate')

    // Commons IO
    compile project(':commons-io')

    ///////////////////////////
    // Third Party Libraries //
    ///////////////////////////

    // About Libraries
    compile('com.mikepenz:aboutlibraries:5.8.0@aar') {
        transitive = true
    }

    // Animated ImageView
    compile project(':scrollingimageview')

    // App Intro
    compile project(':welcome')

    // Blur Effect
    compile project(':blurview')

    // Circular Dialogs
    compile project(':circularfillableloaders')

    // EditTexts
    compile project(':animatededittext')

    // Floating Action Buttons
    compile project(':materialsheetfab')

    // Gesture RecyclerView
    compile project(':gesturerecycler')

    // Ken Burns View Image Transitions
    compile project(':kenburnsview')

    // Material Drawer
    compile('com.mikepenz:materialdrawer:5.5.0@aar') {
        transitive = true
    }

    // Material Progress Bars
    compile project(':materialprogressbar')

    // ZipSigner
    compile files('src/main/libs/kellinwood-logging-android-1.4.jar')
    compile files('src/main/libs/kellinwood-logging-lib-1.1.jar')
    compile files('src/main/libs/kellinwood-logging-log4j-1.0.jar')
    compile files('src/main/libs/zipio-lib-1.8.jar')
    compile files('src/main/libs/zipsigner-lib-1.17.jar')

    // ZT Zip
    compile project(':ztzip')
}

apply plugin: 'com.google.gms.google-services'